@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model Application.GameCQ.Image.Queries.ImageListViewModel
@{
    Layout = "_Layout";
}
<head>
</head>
<body>
    <partial name="_NavBar">
        <div class="container">
            <partial name="_SideBars">
                @foreach (var image in Model.Images)
                {
                    <div class="entry-content single-entry-content">

                        <div class="entry-header with-image full site-background">
                            <div class="wp-block-image is-style-circle-mask"><figure class="aligncenter size-large is-resized"><img src="@image.Path" alt="" width="300" height="300"></figure></div>
                            <h1 class="section-title has-text-align-center image-name">@image.Name</h1>
                            <h1 class="has-luminous-vivid-amber-color has-text-color has-text-align-center image-description">@image.Description</h1>
                        </div>
                        <div class="has-text-align-center race-pick">
                            <form asp-controller="UnitCreation" asp-action="SubmittedCreate" asp-route-id="@image.Name" method="post">
                                <select name="race">
                                    <option value="Human">Human</option>
                                    <option value="Orc">Orc</option>
                                    <option value="Elf">Elf</option>
                                    <option value="Dwarf">Dwarf</option>
                                    <option value="Troll">Troll</option>
                                    <option value="Goblin">Goblin</option>
                                </select>
                                <input type="submit" name="" value="Pick this Class" />
                            </form>
                        </div>
                    </div>
                    <hr />
                    <br />
                }
        </div>

</body>

<partial name="_Footer">